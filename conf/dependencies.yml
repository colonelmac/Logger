# Application dependencies

require:
    - play
    - biz.neustar -> loggly 1.0-beta-5

repositories:
    - mvn:
        type: iBiblio
        root: "http://repo1.maven.org/maven2"
        contains:
            - biz.neustar -> *
